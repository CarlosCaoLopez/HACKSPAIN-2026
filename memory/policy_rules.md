# Reglas aprendidas entre runs

<!-- Lo escribe `core/memory.py` al terminar un run, no una persona.
     Solo entran reglas con soporte en al menos dos runs.
     Este fichero se inyecta tal cual en el prompt del planner del run siguiente.
     El run 1 no tiene memoria y no pasa nada. -->
